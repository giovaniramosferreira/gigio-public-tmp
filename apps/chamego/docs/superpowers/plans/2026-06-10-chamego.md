# Chamego Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar o protótipo em produto vendável: pay-to-publish via Pix (Mercado Pago), Roteiro de Dates com lugares reais (Google Places), IA gerada 1× na criação (Haiku 4.5), redesign editorial mobile-first e deploy no Render.

**Architecture:** Monolito Express (Render Starter + disco persistente) servindo o build Vite e a API no mesmo origin. SQLite (`better-sqlite3`) para páginas/pagamentos; uploads em disco. Todas as chaves de API só no servidor. Frontend usa caminhos relativos `/api/...` (proxy Vite em dev). Spec: `docs/superpowers/specs/2026-06-10-chamego-design.md`. Glossário: `CONTEXT.md`.

**Tech Stack:** React 19 + Vite + Tailwind 3, Express 4, better-sqlite3, multer, Mercado Pago REST (Pix), Google Places API (New), Anthropic Messages API (claude-haiku-4-5-20251001), OpenAI Whisper (opcional), Vitest + Supertest.

---

## Fase 0 — Fundação (app continua rodando após cada task)

### Task 1: Centralizar URLs de API + proxy Vite

**Files:**
- Modify: `vite.config.js`
- Modify: todos os arquivos com `http://localhost:3001` (20 ocorrências em `src/`)

- [ ] **Step 1: Proxy no Vite**

```js
// vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:3001',
      '/uploads': 'http://localhost:3001',
    },
  },
})
```

- [ ] **Step 2: Substituir todas as ocorrências**

Em `src/`: chamadas `fetch('http://localhost:3001/api/X')` viram `fetch('/api/X')`; atributos `src={`http://localhost:3001${url}`}` viram `src={url}`. Verificar:

Run: `grep -rn "localhost:3001" src/ | wc -l` → Expected: `0`

- [ ] **Step 3: Build verde**

Run: `npm run build` → Expected: sucesso.

- [ ] **Step 4: Commit** — `git commit -m "refactor: URLs de API relativas + proxy Vite"`

### Task 2: ESLint limpo

**Files:**
- Modify: `eslint.config.js` (globals Node p/ `backend/**` e `run_e2e_test.js`)
- Modify: arquivos com imports não usados; `src/pages/LandingPage.jsx` (typewriter)

- [ ] **Step 1: Globals Node**

```js
// eslint.config.js — adicionar bloco após o existente
{
  files: ['backend/**/*.js', 'run_e2e_test.js'],
  languageOptions: { globals: globals.node },
},
```

- [ ] **Step 2: Remover imports não usados** apontados por `npx eslint .` (ícones lucide etc.).

- [ ] **Step 3: Typewriter sem setState síncrono em effect** — mover a troca de frase para dentro do próprio `setTimeout`:

```jsx
} else if (isDeleting && currentPhrase === "") {
  timer = setTimeout(() => {
    setIsDeleting(false);
    setPhraseIndex((prev) => (prev + 1) % typewriterPhrases.length);
  }, 300);
}
```

- [ ] **Step 4:** Run: `npx eslint .` → Expected: 0 errors (warnings de deps aceitáveis).
- [ ] **Step 5: Commit** — `chore: lint limpo`

### Task 3: SQLite substitui db.json

**Files:**
- Create: `backend/db.js` (substitui `backend/database.js`)
- Create: `backend/__tests__/db.test.js`
- Modify: `backend/server.js` (import), `package.json`

- [ ] **Step 1:** `npm i better-sqlite3 && npm i -D vitest supertest`
- [ ] **Step 2: Teste falhando**

```js
// backend/__tests__/db.test.js
import { describe, it, expect, beforeEach } from 'vitest';
import { createDb } from '../db.js';

describe('db', () => {
  let db;
  beforeEach(() => { db = createDb(':memory:'); });

  it('salva e lê rascunho por slug', () => {
    db.savePage({ slug: 'a-e-b', data: { titulo: 'A & B' }, status: 'draft' });
    const p = db.getPageBySlug('a-e-b');
    expect(p.status).toBe('draft');
    expect(p.data.titulo).toBe('A & B');
  });

  it('publica página', () => {
    db.savePage({ slug: 'a-e-b', data: {}, status: 'draft' });
    db.publishPage('a-e-b');
    expect(db.getPageBySlug('a-e-b').status).toBe('published');
  });

  it('gera slug único com colisão', () => {
    db.savePage({ slug: 'a-e-b', data: {}, status: 'draft' });
    expect(db.uniqueSlug('A & B')).toBe('a-e-b-2');
  });

  it('registra pagamento e acha por id', () => {
    db.savePayment({ id: '123', slug: 'a-e-b', status: 'pending', amount: 19.9 });
    expect(db.getPayment('123').slug).toBe('a-e-b');
  });
});
```

Run: `npx vitest run backend` → Expected: FAIL (módulo não existe).

- [ ] **Step 3: Implementar**

```js
// backend/db.js
import Database from 'better-sqlite3';
import path from 'path';
import fs from 'fs';

export function slugify(titulo) {
  return titulo.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '')
    .replace(/[^a-z0-9\s-]/g, '').trim().replace(/\s+/g, '-') || 'nosso-amor';
}

export function createDb(file) {
  if (file !== ':memory:') fs.mkdirSync(path.dirname(file), { recursive: true });
  const sqlite = new Database(file);
  sqlite.pragma('journal_mode = WAL');
  sqlite.exec(`
    CREATE TABLE IF NOT EXISTS pages (
      slug TEXT PRIMARY KEY,
      data TEXT NOT NULL,
      status TEXT NOT NULL DEFAULT 'draft',
      email TEXT DEFAULT '',
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now'))
    );
    CREATE TABLE IF NOT EXISTS payments (
      id TEXT PRIMARY KEY,
      slug TEXT NOT NULL,
      status TEXT NOT NULL,
      amount REAL NOT NULL,
      created_at TEXT DEFAULT (datetime('now')),
      updated_at TEXT DEFAULT (datetime('now'))
    );
  `);
  return {
    savePage({ slug, data, status = 'draft', email = '' }) {
      sqlite.prepare(`INSERT INTO pages (slug, data, status, email) VALUES (?, ?, ?, ?)
        ON CONFLICT(slug) DO UPDATE SET data=excluded.data, email=excluded.email, updated_at=datetime('now')`)
        .run(slug, JSON.stringify(data), status, email);
      return this.getPageBySlug(slug);
    },
    getPageBySlug(slug) {
      const row = sqlite.prepare('SELECT * FROM pages WHERE slug = ?').get(slug);
      return row ? { ...row, data: JSON.parse(row.data) } : null;
    },
    publishPage(slug) {
      sqlite.prepare(`UPDATE pages SET status='published', updated_at=datetime('now') WHERE slug=?`).run(slug);
    },
    uniqueSlug(titulo) {
      const base = slugify(titulo);
      let slug = base, n = 2;
      while (this.getPageBySlug(slug)) slug = `${base}-${n++}`;
      return slug;
    },
    savePayment({ id, slug, status, amount }) {
      sqlite.prepare(`INSERT INTO payments (id, slug, status, amount) VALUES (?, ?, ?, ?)
        ON CONFLICT(id) DO UPDATE SET status=excluded.status, updated_at=datetime('now')`)
        .run(String(id), slug, status, amount);
    },
    getPayment(id) { return sqlite.prepare('SELECT * FROM payments WHERE id = ?').get(String(id)); },
  };
}

const DATA_DIR = process.env.DATA_DIR || path.join(path.dirname(new URL(import.meta.url).pathname), 'data');
export const db = createDb(process.env.NODE_ENV === 'test' ? ':memory:' : path.join(DATA_DIR, 'chamego.db'));
```

- [ ] **Step 4:** Run: `npx vitest run backend` → Expected: PASS. Adicionar script `"test": "vitest run"` no package.json.
- [ ] **Step 5:** Trocar imports em `server.js`: `import { db } from './db.js'` e usar `db.getPageBySlug`/`db.savePage`. Deletar `backend/database.js` e `backend/db.json`.
- [ ] **Step 6: Commit** — `feat: SQLite com better-sqlite3 substitui db.json`

### Task 4: Express serve o build + uploads no DATA_DIR

**Files:**
- Modify: `backend/server.js`

- [ ] **Step 1:** `uploadsDir` passa a ser `path.join(process.env.DATA_DIR || __dirname, 'uploads')`.
- [ ] **Step 2: Servir dist com fallback SPA** (depois das rotas `/api` e `/uploads`):

```js
const distDir = path.join(__dirname, '..', 'dist');
if (fs.existsSync(distDir)) {
  app.use(express.static(distDir));
  app.get(/^\/(?!api|uploads).*/, (req, res) => res.sendFile(path.join(distDir, 'index.html')));
}
```

- [ ] **Step 3:** Run: `npm run build && node backend/server.js` + abrir `http://localhost:3001/` → Expected: landing renderiza pelo Express.
- [ ] **Step 4: Commit** — `feat: Express serve build com fallback SPA`

## Fase 1 — Integrações backend

### Task 5: Módulo de IA (Haiku 4.5, geração 1× na criação)

**Files:**
- Create: `backend/ai.js`
- Create: `backend/__tests__/ai.test.js`
- Modify: `backend/server.js` (remover blocos Claude inline duplicados; endpoints passam a usar módulo)

- [ ] **Step 1: Teste falhando** (fallbacks sem chave)

```js
// backend/__tests__/ai.test.js
import { describe, it, expect } from 'vitest';
import { generatePageContent } from '../ai.js';

describe('generatePageContent', () => {
  it('sem ANTHROPIC_API_KEY usa fallbacks e nunca lança', async () => {
    delete process.env.ANTHROPIC_API_KEY;
    const out = await generatePageContent({
      titulo: 'Ana & Léo', dataInicio: '2023-01-10',
      dataNasc1: '1995-04-10', dataNasc2: '1996-08-25',
      mensagem: 'te amo', conquistas: [],
    });
    expect(out.horoscopoTexto.length).toBeGreaterThan(50);
    expect(out.cartaDeAmor).toContain('Ana');
    expect(out.compatibilidade.scoreGeral).toBeGreaterThanOrEqual(75);
    expect(out.signo1.name).toBe('Áries');
  });
});
```

Run: `npx vitest run backend/__tests__/ai.test.js` → FAIL.

- [ ] **Step 2: Implementar `backend/ai.js`**

Mover de `server.js`: `getZodiacSign`, `getSimulatedHoroscope`, fallback da carta (template literal existente), fallback do score (seed determinística existente), templates do Cupido. Criar:

```js
const MODEL = 'claude-haiku-4-5-20251001';

async function callClaude(prompt, maxTokens = 1024) {
  if (!process.env.ANTHROPIC_API_KEY) return '';
  try {
    const res = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'x-api-key': process.env.ANTHROPIC_API_KEY,
        'anthropic-version': '2023-06-01',
        'content-type': 'application/json',
      },
      body: JSON.stringify({ model: MODEL, max_tokens: maxTokens, messages: [{ role: 'user', content: prompt }] }),
    });
    if (!res.ok) { console.error('Claude error', res.status, await res.text()); return ''; }
    const data = await res.json();
    return data.content?.[0]?.text || '';
  } catch (e) { console.error('Claude network', e.message); return ''; }
}

export async function generatePageContent(input) {
  const { titulo, dataInicio, dataNasc1, dataNasc2, mensagem, conquistas } = input;
  const [nome1, nome2] = splitNames(titulo);          // reusar regex /\s+(?:&|e)\s+/i do server.js
  const signo1 = dataNasc1 ? getZodiacSign(dataNasc1) : null;
  const signo2 = dataNasc2 ? getZodiacSign(dataNasc2) : null;

  const [horoscopo, carta, scoreJson] = await Promise.all([
    signo1 && signo2 ? callClaude(promptSinastria({ nome1, nome2, signo1, signo2, dataInicio })) : '',
    callClaude(promptCarta({ titulo, nome1, nome2, dataInicio, mensagem, conquistas }), 1200),
    callClaude(promptScore({ nome1, nome2, signo1, signo2, dataInicio, conquistas, mensagem }), 400),
  ]);

  return {
    signo1, signo2,
    horoscopoTexto: signo1 && signo2 ? (horoscopo || getSimulatedHoroscope(nome1, signo1, nome2, signo2)) : '',
    cartaDeAmor: carta || fallbackCarta({ nome1, nome2, dataInicio, mensagem, conquistas }),
    compatibilidade: parseScore(scoreJson) || fallbackScore({ nome1, nome2, dataInicio }),
  };
}

export async function generateCupido({ transcription, titulo, mensagem }) {
  // prompt atual do Cupido (server.js) via callClaude; fallback: templates atuais (array de 4)
}

export async function generateDatePitch(place) {
  // 1 frase romântica curta por lugar via callClaude (prompt: nome+categoria, pt-BR, máx 20 palavras)
  // fallback: frase fixa por categoria ('Café da manhã' → 'Comecem o dia juntinhos com um café especial.' etc.)
}
```

Prompts `promptSinastria/promptCarta/promptScore`: copiar literalmente os textos dos endpoints atuais de `server.js`. `parseScore` = `JSON.parse` com fallback regex `/\{[\s\S]*\}/` (já existe em server.js).

- [ ] **Step 3:** Run: `npx vitest run` → PASS.
- [ ] **Step 4:** `server.js`: endpoints `/api/ai/love-letter`, `/api/ai/compatibility`, `/api/horoscope/generate` são **removidos** (geração agora interna); `/api/uploads/page-audio` usa `generateCupido`.
- [ ] **Step 5: Commit** — `feat: módulo de IA com Haiku 4.5 e fallbacks`

### Task 6: Google Places — autocomplete + Roteiro de Dates

**Files:**
- Create: `backend/places.js`
- Create: `backend/__tests__/places.test.js`
- Modify: `backend/server.js` (rotas)

- [ ] **Step 1: Teste falhando** (fetch mockado)

```js
// backend/__tests__/places.test.js
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { buildRoteiro } from '../places.js';

const fakePlace = (name) => ({
  displayName: { text: name }, rating: 4.7, priceLevel: 'PRICE_LEVEL_MODERATE',
  googleMapsUri: 'https://maps.google.com/?cid=1',
  photos: [{ name: 'places/abc/photos/xyz' }], formattedAddress: 'Rua X, 1',
});

beforeEach(() => {
  process.env.GOOGLE_MAPS_API_KEY = 'test-key';
  global.fetch = vi.fn(async () => ({ ok: true, json: async () => ({ places: [fakePlace('Café Lindo')] }) }));
});

it('monta roteiro com seções dia e noite', async () => {
  const r = await buildRoteiro({ cidade: 'São Paulo', bairro: 'Pinheiros' });
  expect(r.dia.length).toBeGreaterThan(0);
  expect(r.noite.length).toBeGreaterThan(0);
  expect(r.dia[0]).toMatchObject({ nome: 'Café Lindo', nota: 4.7 });
  expect(r.dia[0].fotoUrl).toContain('/api/places/photo?name=');
});
```

Run → FAIL.

- [ ] **Step 2: Implementar `backend/places.js`**

```js
const KEY = () => process.env.GOOGLE_MAPS_API_KEY;
const BASE = 'https://places.googleapis.com/v1';
const FIELDS = 'places.displayName,places.rating,places.priceLevel,places.googleMapsUri,places.photos,places.formattedAddress';

async function searchText(textQuery) {
  const res = await fetch(`${BASE}/places:searchText`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'X-Goog-Api-Key': KEY(), 'X-Goog-FieldMask': FIELDS },
    body: JSON.stringify({ textQuery, languageCode: 'pt-BR', maxResultCount: 3 }),
  });
  if (!res.ok) { console.error('Places error', res.status, await res.text()); return []; }
  return (await res.json()).places || [];
}

const PRECO = {
  PRICE_LEVEL_INEXPENSIVE: '$', PRICE_LEVEL_MODERATE: '$$',
  PRICE_LEVEL_EXPENSIVE: '$$$', PRICE_LEVEL_VERY_EXPENSIVE: '$$$$',
};

const toCard = (p, categoria) => ({
  categoria,
  nome: p.displayName?.text || '',
  nota: p.rating ?? null,
  preco: PRECO[p.priceLevel] || '',
  endereco: p.formattedAddress || '',
  mapsUrl: p.googleMapsUri || '',
  fotoUrl: p.photos?.[0]?.name ? `/api/places/photo?name=${encodeURIComponent(p.photos[0].name)}` : '',
});

export async function buildRoteiro({ cidade, bairro }) {
  const loc = `${bairro}, ${cidade}`;
  const [cafes, passeios, restaurantes, bares] = await Promise.all([
    searchText(`café charmoso para casal em ${loc}`),
    searchText(`parque ou passeio romântico em ${loc}`),
    searchText(`restaurante romântico jantar em ${loc}`),
    searchText(`bar aconchegante para casal em ${loc}`),
  ]);
  return {
    dia: [...cafes.map(p => toCard(p, 'Café da manhã')), ...passeios.map(p => toCard(p, 'Passeio'))].slice(0, 4),
    noite: [...restaurantes.map(p => toCard(p, 'Jantar')), ...bares.map(p => toCard(p, 'Drinks'))].slice(0, 4),
  };
}

export async function autocomplete(input) {
  const res = await fetch(`${BASE}/places:autocomplete`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'X-Goog-Api-Key': KEY() },
    body: JSON.stringify({ input, languageCode: 'pt-BR', includedRegionCodes: ['br'] }),
  });
  if (!res.ok) return [];
  const data = await res.json();
  return (data.suggestions || []).map(s => s.placePrediction?.text?.text).filter(Boolean);
}

export async function photoStream(name, maxWidth = 900) {
  return fetch(`https://places.googleapis.com/v1/${name}/media?maxWidthPx=${maxWidth}&key=${KEY()}`);
}
```

Rotas em `server.js`:

```js
app.get('/api/places/autocomplete', async (req, res) => {
  res.json({ suggestions: await autocomplete(String(req.query.q || '')) });
});

app.post('/api/places/roteiro', async (req, res) => {
  const { cidade, bairro } = req.body;
  if (!cidade || !bairro) return res.status(400).json({ error: 'Informe cidade e bairro' });
  const roteiro = await buildRoteiro({ cidade, bairro });
  for (const sec of ['dia', 'noite']) {
    await Promise.all(roteiro[sec].map(async c => { c.frase = await generateDatePitch(c); }));
  }
  res.json(roteiro);
});

app.get('/api/places/photo', async (req, res) => {
  const upstream = await photoStream(String(req.query.name || ''));
  if (!upstream.ok) return res.status(404).end();
  res.set('Content-Type', upstream.headers.get('content-type') || 'image/jpeg');
  res.set('Cache-Control', 'public, max-age=86400');
  res.send(Buffer.from(await upstream.arrayBuffer()));
});
```

Remover endpoint antigo `/api/dates/suggest` e `getSimulatedDateSuggestions` (substituídos).

- [ ] **Step 3:** Run: `npx vitest run` → PASS.
- [ ] **Step 4: Commit** — `feat: Google Places — autocomplete, roteiro dia/noite e proxy de fotos`

### Task 7: Rascunhos (`POST /api/drafts`) com IA na criação

**Files:**
- Modify: `backend/server.js` (exportar `app`: `export const app` + `if (process.env.NODE_ENV !== 'test') app.listen(...)`)
- Create: `backend/__tests__/drafts.test.js`

- [ ] **Step 1: Teste falhando**

```js
// backend/__tests__/drafts.test.js
import { it, expect } from 'vitest';
import request from 'supertest';
import { app } from '../server.js';

it('cria rascunho com slug bonito e conteúdo IA (fallback)', async () => {
  const res = await request(app).post('/api/drafts').send({
    data: { titulo: 'Ana & Léo', dataInicio: '2023-01-10', mensagem: 'te amo' },
  });
  expect(res.status).toBe(200);
  expect(res.body.slug).toBe('ana-e-leo');
  expect(res.body.page.data.cartaDeAmor.length).toBeGreaterThan(50);
});

it('rascunho não abre como página pública', async () => {
  const res = await request(app).get('/api/pages/ana-e-leo');
  expect(res.status).toBe(403);
  expect(res.body.status).toBe('draft');
});
```

Run → FAIL.

- [ ] **Step 2: Implementar**

```js
app.post('/api/drafts', async (req, res) => {
  try {
    const { data, email } = req.body;
    if (!data?.titulo || !data?.dataInicio) return res.status(400).json({ error: 'Faltam título ou data de início' });
    const existing = data.slug ? db.getPageBySlug(data.slug) : null;
    const slug = existing && existing.status === 'draft' ? data.slug : db.uniqueSlug(data.titulo);
    const ai = await generatePageContent(data);
    const page = db.savePage({ slug, email: email || '', status: 'draft', data: { ...data, ...ai, slug } });
    res.json({ slug, page });
  } catch (e) {
    console.error('draft error', e);
    res.status(500).json({ error: 'Erro ao salvar rascunho' });
  }
});

app.get('/api/pages/:slug', (req, res) => {
  const page = db.getPageBySlug(req.params.slug);
  if (!page) return res.status(404).json({ error: 'Página não encontrada' });
  if (page.status !== 'published') return res.status(403).json({ status: 'draft', error: 'Página aguardando pagamento' });
  res.json({ ...page.data, status: page.status });
});
```

Remover endpoint antigo `/api/orders`.

- [ ] **Step 3:** Run: `npx vitest run` → PASS.
- [ ] **Step 4: Commit** — `feat: rascunhos com geração de IA na criação; página pública exige publicação`

### Task 8: Pagamento Pix (Mercado Pago) + webhook de Publicação

**Files:**
- Create: `backend/payments.js`
- Create: `backend/__tests__/payments.test.js`
- Modify: `backend/server.js`

- [ ] **Step 1: Teste falhando** (fetch mockado)

```js
// backend/__tests__/payments.test.js
import { it, expect, vi, beforeEach } from 'vitest';
import request from 'supertest';
import { app } from '../server.js';
import { db } from '../db.js';

beforeEach(() => {
  process.env.MP_ACCESS_TOKEN = 'TEST-token';
  db.savePage({ slug: 'casal-pix', data: { titulo: 'C & P' }, status: 'draft' });
});

it('cria pagamento pix e retorna qr', async () => {
  global.fetch = vi.fn(async () => ({ ok: true, json: async () => ({
    id: 555, status: 'pending',
    point_of_interaction: { transaction_data: { qr_code: 'copia-cola', qr_code_base64: 'aGVsbG8=' } },
  }) }));
  const res = await request(app).post('/api/payments').send({ slug: 'casal-pix', email: 'a@b.c' });
  expect(res.status).toBe(200);
  expect(res.body).toMatchObject({ paymentId: '555', qrCode: 'copia-cola', qrCodeBase64: 'aGVsbG8=' });
});

it('webhook aprovado publica página', async () => {
  global.fetch = vi.fn(async () => ({ ok: true, json: async () => ({
    id: 555, status: 'approved', external_reference: 'casal-pix',
  }) }));
  const res = await request(app).post('/api/webhooks/mercadopago').send({ type: 'payment', data: { id: 555 } });
  expect(res.status).toBe(200);
  expect(db.getPageBySlug('casal-pix').status).toBe('published');
});
```

Run → FAIL.

- [ ] **Step 2: Implementar `backend/payments.js`**

```js
import crypto from 'crypto';

const MP = 'https://api.mercadopago.com';
const headers = () => ({
  Authorization: `Bearer ${process.env.MP_ACCESS_TOKEN}`,
  'Content-Type': 'application/json',
});

export const PRICE = 19.9;

export async function createPixPayment({ slug, email }) {
  const res = await fetch(`${MP}/v1/payments`, {
    method: 'POST',
    headers: { ...headers(), 'X-Idempotency-Key': crypto.randomUUID() },
    body: JSON.stringify({
      transaction_amount: PRICE,
      payment_method_id: 'pix',
      description: `Chamego — Página do Casal (${slug})`,
      external_reference: slug,
      payer: { email: email || 'pagador@chamego.app' },
      notification_url: process.env.PUBLIC_URL ? `${process.env.PUBLIC_URL}/api/webhooks/mercadopago` : undefined,
    }),
  });
  if (!res.ok) throw new Error(`MP ${res.status}: ${await res.text()}`);
  return res.json();
}

export async function getPayment(id) {
  const res = await fetch(`${MP}/v1/payments/${id}`, { headers: headers() });
  if (!res.ok) throw new Error(`MP ${res.status}`);
  return res.json();
}
```

Rotas em `server.js`:

```js
app.post('/api/payments', async (req, res) => {
  try {
    const { slug, email } = req.body;
    const page = db.getPageBySlug(slug);
    if (!page) return res.status(404).json({ error: 'Rascunho não encontrado' });
    if (page.status === 'published') return res.json({ alreadyPublished: true });
    const p = await createPixPayment({ slug, email });
    db.savePayment({ id: p.id, slug, status: p.status, amount: PRICE });
    const tx = p.point_of_interaction?.transaction_data || {};
    res.json({ paymentId: String(p.id), qrCode: tx.qr_code, qrCodeBase64: tx.qr_code_base64 });
  } catch (e) { console.error(e); res.status(502).json({ error: 'Falha ao criar pagamento Pix' }); }
});

app.get('/api/payments/:id/status', async (req, res) => {
  const local = db.getPayment(req.params.id);
  if (!local) return res.status(404).json({ error: 'Pagamento não encontrado' });
  if (local.status !== 'approved') {
    try {
      const p = await getPayment(req.params.id);
      db.savePayment({ id: p.id, slug: local.slug, status: p.status, amount: local.amount });
      if (p.status === 'approved') db.publishPage(local.slug);
      return res.json({ status: p.status, slug: local.slug });
    } catch { /* devolve estado local abaixo */ }
  }
  res.json({ status: db.getPayment(req.params.id).status, slug: local.slug });
});

app.post('/api/webhooks/mercadopago', async (req, res) => {
  try {
    const id = req.body?.data?.id || req.query['data.id'];
    if (req.body?.type === 'payment' || req.query.type === 'payment') {
      const p = await getPayment(id);
      if (p.external_reference) {
        db.savePayment({ id: p.id, slug: p.external_reference, status: p.status, amount: PRICE });
        if (p.status === 'approved') db.publishPage(p.external_reference);
      }
    }
    res.sendStatus(200); // sempre 200 — MP reenvia em caso de erro
  } catch (e) { console.error('webhook', e); res.sendStatus(200); }
});
```

- [ ] **Step 3:** Run: `npx vitest run` → PASS.
- [ ] **Step 4: Commit** — `feat: pagamento Pix Mercado Pago + webhook publica página`

## Fase 2 — Frontend (redesign editorial mobile-first)

### Task 9: Design system

**Files:**
- Modify: `tailwind.config.js`, `src/index.css`, `index.html`

- [ ] **Step 1: Tokens**

```js
// tailwind.config.js — theme.extend
colors: {
  cream: { 50: '#FAF7F2', 100: '#F3EDE3', 200: '#E8DFD0' },
  ink: { 900: '#1A1714', 600: '#5C554C', 400: '#948C80' },
  wine: { 700: '#B3284F', 600: '#C9355F', 100: '#F7E3E9' },
},
fontFamily: {
  display: ['Fraunces', 'Georgia', 'serif'],
  sans: ['Inter', 'system-ui', 'sans-serif'],
},
```

```css
/* src/index.css — base (substitui estilos legados) */
@import url('https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,600;9..144,700&family=Inter:wght@400;500;600;700&display=swap');
@tailwind base; @tailwind components; @tailwind utilities;

body { @apply bg-cream-50 text-ink-900 font-sans antialiased; }

.btn-primary { @apply inline-flex items-center justify-center gap-2 rounded-full bg-wine-700 text-white font-semibold px-7 py-4 text-base transition-all active:scale-[0.98] hover:bg-wine-600 shadow-sm min-h-[52px]; }
.btn-ghost   { @apply inline-flex items-center justify-center gap-2 rounded-full border border-ink-900/15 bg-white/60 text-ink-900 font-semibold px-6 py-3.5 min-h-[48px]; }
.input-base  { @apply w-full rounded-2xl border border-ink-900/15 bg-white px-5 py-4 text-base text-ink-900 placeholder:text-ink-400 focus:outline-none focus:border-wine-700 min-h-[52px]; }
.card        { @apply bg-white rounded-3xl border border-ink-900/10 shadow-[0_1px_3px_rgba(26,23,20,0.06)]; }
.eyebrow     { @apply text-[11px] font-semibold uppercase tracking-[0.18em] text-ink-400; }
```

Classes inválidas legadas (`text-slate-805`, `border-pink-150`, `bg-sugar-50` etc.) morrem conforme páginas são reescritas.

- [ ] **Step 2:** `index.html`: `<title>Chamego — presenteie quem você ama</title>` + `<meta name="description" content="Crie uma página de amor com fotos, música, contador e roteiro de dates reais. Pague R$19,90 no Pix e compartilhe por link ou QR Code.">`.
- [ ] **Step 3:** `npm run build` verde. **Commit** — `feat: design system editorial (Fraunces/Inter, creme, vinho)`

### Task 10: Header/Footer + Landing editorial

**Files:**
- Modify: `src/components/Header.jsx`, `src/components/Footer.jsx`, `src/pages/LandingPage.jsx`

- [ ] **Step 1: Header** — barra creme translúcida fixa: wordmark `Chamego` em `font-display italic` vinho; CTA `btn-primary` compacto "Criar página". Sem menu hambúrguer (links de âncora só no footer). Mobile: logo + CTA.
- [ ] **Step 2: Footer** — wordmark, "Feito com ❤️ no Brasil", © 2026 Chamego. Sem redes sociais.
- [ ] **Step 3: Landing** — seções:
  1. **Hero**: eyebrow "presente digital", H1 `font-display` 44–64px "Sua história de amor merece uma página", subtítulo 1 linha, CTA único "Criar nossa página — R$19,90", foto grande (usar `src/assets/hero.png`) full-width com cantos 32px.
  2. **Como funciona**: 3 passos, números grandes em Fraunces, copy atual mantida.
  3. **Cenas**: grid (1 col mobile / 2 col desktop) dos 8 itens de `scenes` — foto grande, título serif, descrição. Sem mockup de celular fake.
  4. **Depoimentos**: 4 cards brancos limpos (copy atual).
  5. **Preço**: card único centrado — ~~R$39,90~~ **R$19,90** · pagamento único via Pix · página vitalícia · lista: todas as cenas, carta de amor IA, roteiro de dates reais, retrospectiva. CTA.
  6. **FAQ**: atualizar — remover plano 24h e "alterar depois"; adicionar "Como pago? Pix com aprovação na hora" e "Posso excluir? Sim, é só pedir no suporte".
- [ ] **Step 4:** `npx eslint . && npm run build` verdes. **Commit** — `feat: landing editorial mobile-first com preço único`

### Task 11: Wizard novo (4+1 passos) em `/criar`

**Files:**
- Modify: `src/pages/CreatorWizard.jsx` (reescrita), `src/App.jsx` (rotas)
- Delete: `src/pages/UpsellPage.jsx`, `src/pages/EmailStep.jsx`

**Rotas novas em App.jsx:** `/`, `/criar`, `/criar/checkout`, `/p/:slug`; `<Route path="/classico/criar" element={<Navigate to="/criar" replace />} />`. Remover rotas de upsell/email e `/pt/:slug`.

- [ ] **Step 1: Passos** — manter handlers existentes (upload fotos/áudio, gravação, iTunes, conquistas, roleta — já funcionam); reorganizar `renderStepFields`:
  - **1 · Vocês**: `titulo`, `dataInicio`; `dataNasc1/2` com rótulo "opcional — habilita a Sinastria";
  - **2 · Fotos**: dropzone grande + grid de miniaturas com remover;
  - **3 · Mensagem**: textarea grande;
  - **4 · Onde vocês vivem**: input autocomplete → `GET /api/places/autocomplete?q=` (debounce 300ms, lista clicável que preenche `cidade`/`bairro`) + botão "Gerar roteiro de dates" → `POST /api/places/roteiro` → preview dos cards Dia/Noite (estado `formData.roteiro`);
  - **5 · Toques finais** (acordeões, tudo opcional): música, áudio do casal, palavra secreta, roleta, conquistas;
  - **6 · Revisar**: resumo visual + "Continuar para pagamento".
- [ ] **Step 2: Submit** — `POST /api/drafts` com `{ data: formData, email: '' }`; salvar `slug` em `sessionStorage('chamego-slug')`; `navigate('/criar/checkout?slug=' + slug)`.
- [ ] **Step 3: Visual** — design system: barra de progresso fina vinho, título do passo em Fraunces, `input-base`, botão de avançar fixo no rodapé mobile (respeitando `env(safe-area-inset-bottom)`), alvos ≥44px.
- [ ] **Step 4:** Teste manual `npm run dev` (rascunho criado com IA fallback sem chaves). `npx eslint .` verde.
- [ ] **Step 5: Commit** — `feat: wizard 4+1 passos com autocomplete e roteiro de dates real`

### Task 12: Checkout Pix novo + tela de sucesso

**Files:**
- Modify: `src/pages/CheckoutPage.jsx` (reescrita)
- Dep: `npm i qrcode.react`

- [ ] **Step 1: Pagamento** — resumo (Página do Casal · **R$19,90** · Pix), campo e-mail opcional ("para recibo"), botão "Gerar Pix" → `POST /api/payments {slug, email}` → exibir `<img src={'data:image/png;base64,' + qrCodeBase64}>` + copia-e-cola com botão copiar. Remover método cartão e botão "Simular Confirmação".
- [ ] **Step 2: Polling** — `setInterval` 4s `GET /api/payments/:id/status`; em `approved`: confete, `localStorage.setItem('couple-page-last', slug)`, render sucesso.
- [ ] **Step 3: Sucesso** — link público grande com copiar; QR do link `<QRCodeCanvas value={url} size={220}/>` + botão "Baixar QR" (canvas → `toDataURL` → `<a download>`); botão "Compartilhar no WhatsApp" → `https://wa.me/?text=` + `encodeURIComponent('Fiz uma surpresa pra você 💌 ' + url)`; botão "Ver minha página".
- [ ] **Step 4: Banner de retorno** — na Landing: se `localStorage('couple-page-last')`, barra discreta "💌 Sua página está no ar — abrir".
- [ ] **Step 5:** Lint+build verdes. **Commit** — `feat: checkout Pix com QR, polling e sucesso com compartilhamento`

### Task 13: Página do Casal editorial + integração dos órfãos

**Files:**
- Modify: `src/pages/CouplePage.jsx`, `src/components/RetrospectiveStories.jsx`, `src/components/CompatibilityCard.jsx`
- Use: `src/components/StarMapCard.jsx` (como está)
- Delete: `src/components/LoveLetterCard.jsx` (carta vira seção estática)

- [ ] **Step 1: Bugs da Retrospectiva** — `renderHoroscope`: `{data.signo1?.symbol}` e `{data.signo2?.symbol}` (hoje renderiza objeto → crash); `renderCupid`: trocar `{data.conquistas}` por `{data.conquistas.length} conquistas juntos` (hoje renderiza array de objetos → crash); `Particles`/`Stars`: trocar `useRef(Array.from(...))` por `useMemo(() => Array.from(...), [])`.
- [ ] **Step 2: CompatibilityCard display-only** — recebe `compatibilidade` via prop; remover fetch e botão "Analisar".
- [ ] **Step 3: CouplePage** — ordem da spec §3, visual editorial (creme, títulos serif, fotos full-width grandes no lugar da polaroid pequena). Novas seções condicionais: Carta de Amor (`cartaDeAmor`), Score (`<CompatibilityCard compatibilidade={...}>`), Mapa Estelar (`<StarMapCard dataInicio cidade bairro nomes={titulo}>`), Roteiro de Dates (duas colunas Dia ☀️ / Noite 🌙 de cards com `fotoUrl`, nome, ⭐ `nota`, `preco`, `frase`, link `mapsUrl`), botão flutuante "▶ Nossa retrospectiva" abrindo `RetrospectiveStories` (`isOpen` state). Resposta `403` do fetch → tela "Página aguardando pagamento" com CTA "/criar/checkout?slug=".
- [ ] **Step 4:** Teste manual completo em dev. Lint+build verdes.
- [ ] **Step 5: Commit** — `feat: página do casal editorial com carta, score, mapa estelar, roteiro e retrospectiva`

### Task 14: Faxina final

- [ ] Deletar `run_e2e_test.js`, `dummy.jpg`, `dummy.wav`, `src/App.css` (se sem uso), componentes mortos. `npx eslint .` → 0 problemas; `npx vitest run` → verde; `npm run build` → verde.
- [ ] README novo: o que é o Chamego, stack, como rodar (`npm run dev`), env vars (tabela), deploy Render.
- [ ] **Commit** — `chore: remove código morto e atualiza README`

## Fase 3 — Lançamento

### Task 15: GitHub

- [ ] `gh auth status` → se deslogado, usuário roda `gh auth login`.
- [ ] `gh repo create chamego --private --source=. --push`
- [ ] Verificar: `gh repo view chamego`

### Task 16: Render + Mercado Pago

**Files:** Create: `render.yaml`, `.env.example`

- [ ] **Step 1:**

```yaml
# render.yaml
services:
  - type: web
    name: chamego
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: node backend/server.js
    disk:
      name: chamego-data
      mountPath: /var/data
      sizeGB: 5
    envVars:
      - key: DATA_DIR
        value: /var/data
      - key: NODE_VERSION
        value: '22'
```

`.env.example`:

```
MP_ACCESS_TOKEN=
GOOGLE_MAPS_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
PUBLIC_URL=
DATA_DIR=
```

- [ ] **Step 2 (usuário no dashboard):** Render → New → Blueprint → repo `chamego`; preencher env vars com tokens reais; `PUBLIC_URL=https://chamego.onrender.com`.
- [ ] **Step 3:** Painel Mercado Pago → Suas integrações → Webhooks → URL `https://chamego.onrender.com/api/webhooks/mercadopago`, evento Pagamentos.
- [ ] **Step 4: E2E sandbox** — criar página com credenciais de teste MP, pagar com usuário de teste, conferir Publicação automática; depois trocar para produção e validar 1 venda real.
- [ ] **Step 5: Commit + push** — `chore: blueprint Render e env example`

## Self-review (feito)

- **Cobertura da spec:** jornada (T10–T13), backend (T3–T8), IA 1× na criação (T5/T7), Places (T6), MP+webhook (T8), redesign (T9–T13), bugs conhecidos (T1, T2, T13), faxina (T14), infra (T15–T16). Exclusão via WhatsApp = processo manual, sem código.
- **Placeholders:** passos de backend têm código completo; tasks de UI definem estrutura, copys e tokens — JSX final segue o design system da T9.
- **Consistência de nomes:** `db.savePage/getPageBySlug/publishPage/uniqueSlug/savePayment/getPayment`; campos `cartaDeAmor`, `compatibilidade`, `horoscopoTexto`, `roteiro{dia,noite}`, `signo1/2` idênticos em T5/T7/T12/T13.
